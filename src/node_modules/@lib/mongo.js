const mongo = require('mongodb');
import config from './config'

let client = null;
let db = null;
const uri = `mongodb+srv://bangimi:tiffany13@myweb-quxrg.mongodb.net/test?retryWrites=true&w=majority`

export async function init() {
    if(!client) {
        client = await mongo.MongoClient.connect(config.mongo.uri, config.mongo.options)
        db = client.db('personalweb')
    }
    return {client, db}
}